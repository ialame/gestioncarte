# GestionCarte - Docker Setup

##  Configuration Files

| File | Location |
|------|----------|
| `docker-compose.yml` | `/` |
| `settings.xml` | `/` | 
| `localdocker/` | `/` |
| `application-local.properties` | `src/main/resources/` |


##  Running SYF Docker

```bash
#  docker compose down && docker compose up -d
```

##  Running RET Docker

```bash
#  docker compose down && docker compose build --no-cache && docker compose up -d
```
---

